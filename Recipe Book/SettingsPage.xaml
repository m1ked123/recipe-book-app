<Page
    x:Class="Recipe_Book.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Recipe_Book"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CommandBar>
            <AppBarButton Icon="Add" Label="Add Unit of Measure" AllowFocusOnInteraction="True" ToolTipService.ToolTip="Add Unit of Measure" Click="addUnitOfMeasure" />
        </CommandBar>
        <ListView x:Name="unitsOfMeasureList" Grid.Row="1" />
    </Grid>
</Page>
